- name: Bootstrap Kong database
  hosts: kong
  become: true
  gather_facts: false

  tasks:
    - name: Run Kong migrations bootstrap
      command: kong migrations bootstrap
      register: bootstrap_out
      failed_when: false

    - name: Start Kong after bootstrap
      command: kong start
